<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NshutiPlanner" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.NshutiPlanner" parent="Base.Theme.NshutiPlanner" />

    <!-- Splash theme: dark background so there's no white flash on launch -->
    <style name="Theme.NshutiPlanner.Splash" parent="Theme.NshutiPlanner">
        <item name="android:windowBackground">@color/splash_bg</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>